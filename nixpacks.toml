[phases.install]
cmds = ["npm ci", "PLAYWRIGHT_BROWSERS_PATH=0 npx playwright install chromium --with-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "PLAYWRIGHT_BROWSERS_PATH=0 node dist/index.js"
